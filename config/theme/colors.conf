# Sevastopol Theme Palette
# ------------------------
# Background: Deep Void (Not pure black, slightly warm)
$bg = rgb(0d0d0d)

# Foreground: Tungsten (Off-white, not blinding)
$fg = rgb(e0e0e0)

# Accent: Safety Amber (CRT Monochrome feel)
$accent = rgb(ffb86c)

# Dim: De-powered systems
$dim = rgb(444444)

# Alert: Hull Breach Red
$alert = rgb(ff5555)

# Borders
$border_active = $accent
$border_inactive = $dim
